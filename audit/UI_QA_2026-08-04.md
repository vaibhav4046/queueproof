# QueueProof visual QA — 4 August 2026

The final local release candidate was exercised through the browser surface after the
judge-flow rebuild. The `/demo` page reported no horizontal page overflow, broken images,
or out-of-viewport interactive controls at every exact viewport below.

| Viewport | Result | Notes |
| --- | --- | --- |
| 320×568 | pass | seven destinations present; 46px nav targets; no clipped controls |
| 375×667 | pass | proof console and landing sequence stay in bounds |
| 390×844 | pass | no overflow; seven destinations present |
| 430×932 | pass | no overflow; complete proof path |
| 768×1024 | pass | tablet reading order preserved |
| 1024×768 | pass | desktop evidence stage fits |
| 1280×800 | pass | no overflow or broken assets |
| 1440×900 | pass | full proof composition fits |
| 1920×1080 | pass | max-widths prevent overextension |
| 844×390 landscape | pass | proof scene hides; working console remains complete |

Additional route checks:

- `/method`: pass at 320×568, 768×1024, and 1440×900.
- `/benchmarks`: pass at 320×568, 768×1024, and 1440×900.
- The initial 320px benchmark run exposed a clipped **thinking** filter. The final CSS
  wraps the label and three equal controls; the retest reported all controls within the
  305px content viewport.
- A 640×400 effective CSS viewport was checked as the 200% zoom reflow equivalent of a
  1280×800 baseline; all seven destinations remained present and no interactive element
  crossed the content viewport.

Interaction checks:

- `/demo`, `/method`, and `/benchmarks` cold-load with the correct release marker.
- The compiled Cloudflare/Vinext artifact was served with Wrangler against a disposable
  local D1 workspace in explicit public-demo mode. `npm run test:e2e` passed the live
  shell, all seven destinations, direct judge routes, proof-first layout, timeline,
  promised-versus-actual comparison, replay, citations, dialogs, and result-state
  contracts. This was a production-build check, not a development-server proxy.
- Replay loaded six measured rows on the local artifact, advanced from checkpoint 1 to
  2 with **Step**, and **Play again** correctly reset checkpoint 7 to 1.
- The direct replay screen had no out-of-viewport controls at 320×568.
- Browser logs on a fresh tab contained Vite connection and React development notices
  only: zero QueueProof warnings and zero errors.

Fallback and motion checks:

- The proof model is always rendered by the semantic SVG/DOM Evidence Orbit; the removed
  optional Three.js overlay cannot block content or emit its former clock warning.
- The global shader caps rendering at roughly 30fps, pauses when hidden/out of view, and
  renders one static frame under `prefers-reduced-motion`.
- Global reduced-motion CSS disables reveals, drift, sheen, transitions, and smooth
  scroll. The previously captured 390×844 reduced-motion browser receipt remains in
  `audit/screenshots/premium-cdp-390x844-reduced.png`; the final change only removes an
  additional moving layer.
- `tests/shader-background.test.ts` forces a missing WebGL context and verifies that the
  canvas marks itself unavailable so the carbon/CSS background and semantic proof scene
  remain usable.
