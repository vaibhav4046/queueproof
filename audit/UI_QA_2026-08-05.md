# QueueProof production UI QA — 5 August 2026

## Target

- Canonical URL: <https://queueproof.vercel.app>
- Measured runtime: `aed027879150e3e324b54c5ec2194d4d715c501e` on `main`
- Design marker: `ember-assistant-v1`
- Browser console warnings/errors observed during the final pass: **0**

## Responsive layout

| Viewport | Result | Evidence observed |
| --- | --- | --- |
| 1280×800 | **PASS** | 264 px fixed sidebar, mobile dock hidden, 4 sources ready, composer visible, no horizontal overflow |
| 390×844 | **PASS** | `Ask your work. Get the proof.` visible, four-item mobile dock, 58 px dock targets, composer visible, no horizontal overflow |
| 360×800 | **PASS** | mobile dock and single-column layout fit without horizontal overflow |
| 320×568 | **PASS** | smallest tested portrait layout fits without horizontal overflow |
| 844×390 | **PASS** | landscape layout fits without horizontal overflow |

The production browser was also checked after repeated resize transitions; desktop and mobile
navigation swapped at the expected responsive boundary without a stale overlay or page-width
leak.

## Dialog and keyboard behavior

- Opened a real ready-source detail sheet from **Sources**.
- Desktop sheet stayed inside the 1280×800 viewport at 472×800.
- Mobile sheet stayed bounded to the 390×844 viewport with no document overflow.
- Initial focus moved to **Close connection details**.
- `Escape` closed the sheet.
- Focus returned to the originating **Details** button.

## Product-state checks

- Production showed four ready sources and one degraded duplicate source kept out of answers.
- The 346-page PDF displayed page count, SHA-256 prefix, HydraDB source ID, database, and indexed state.
- Direct routes for Ask, Today, Sources, History, Connect AI, Review actions, Proof tests,
  Method, and Owner returned successfully in the release gate.
- Anonymous `/mcp` access remained bearer-protected; the authenticated MCP contract is covered
  by the 12-test MCP suite.

## Boundaries

- Browser zoom percentage was not directly observable through the in-app browser API. Reflow
  was instead exercised down to a 320 CSS-pixel viewport; the automated acceptance suite also
  asserts the reduced-motion and responsive contracts in source.
- This receipt covers runtime A. Evidence build B bundles benchmark artifacts and documents;
  its exact deployed SHA must still be checked through `/api/health/live`.
